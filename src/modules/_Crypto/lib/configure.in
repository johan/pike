# $Id$
AC_INIT(sha.c)

MY_AC_PROG_CC
AC_PROG_CPP
AC_PROG_RANLIB

CROSS="$ac_cv_prog_cc_cross"
# newer autoconf
if test x"$CROSS" = x; then
   CROSS="$cross_compiling"
fi
AC_SUBST(CROSS)

AC_C_CONST

AC_OUTPUT(Makefile)
