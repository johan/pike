test_true([["$Id$"]]);

test_any([[
  object s = ADT.Stack();
  s->push(1);
  return s->pop();
]], 1)

test_any([[
  object s = ADT.Stack();
  s->push(2);
  s->push(3);
  s->push(4);
  s->pop(2);
  return s->pop();
]], 2)

test_any([[
  object s = ADT.Stack();
  s->push(5);
  return s->top();
]], 5)
